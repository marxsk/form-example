Name: {{model.name}}

<hr />
Form:
{{#form-for (changeset model) as |f|}}
  {{f.text-field "name" hint="You have to enter name" label="Das name"}}
  {{f.text-field "city"}}
  {{f.text-field "author" placeholder="123"}}
  {{f.select-field "enabled" "default true false"}}

  {{f.submit "Create account"}}
{{/form-for}}
