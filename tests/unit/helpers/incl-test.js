
import { incl } from 'form-example2/helpers/incl';
import { module, test } from 'qunit';

module('Unit | Helper | incl');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = incl([42]);
  assert.ok(result);
});

